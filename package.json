{
  "name": "simple-queue-test",
  "version": "1.0.0",
  "description": "Test app for simple simplequeue package ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "lcruz",
  "license": "ISC",
  "dependencies": {
    "simplequeue": "0.0.4",
    "webworker-threads": "^0.6.2"
  }
}
